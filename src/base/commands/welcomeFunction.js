module.exports = class WelcomeFunction {
    /**
     *
     * @param {import('../PrismoClient')} client
     */
    constructor(client) {
        this.client = client;
    }
    async setPreset(message, args) {}
};
